root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ."
  entrypoint = "./tmp/main"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  poll = true
  poll_interval = 1000

[misc]
  clean_on_exit = true